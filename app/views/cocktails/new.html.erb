<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Create cocktail</title>
  </head>
  <body>
    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('/assets/carousel_mixology.jpg');">
    <div class="container">

      <div class="container-left">
        <h1>Create your cocktail</h1>
      </div>

      <div class="box-container">
        <%= simple_form_for [@cocktail] do |f|%>
            <%= f.input :name, label:"Write your cocktail name" %>
            <%= f.input :image, label:"Set an image (enter the link)" %>
            <%= f.submit "Let's go!", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </body>
</html>
