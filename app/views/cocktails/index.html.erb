<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Document</title>
  </head>
  <body>
    <div class="container">
      <div class="headline">
        <h1>Cocktails are life</h1>
        <%= link_to "Add a cocktail", new_cocktail_path, class:"btn btn-black" %>
      </div>

      <div class="row">
      <% @cocktails.each do |cocktail| %>
        <%= link_to cocktail_path(cocktail.id), class:'col-sm-12 col-md-6 col-xl-4' do %>
          <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cocktail.image %>')"><%= cocktail.name.capitalize %></div>
          <% end %>
        <% end %>
      </div>
  </body>
</html>

