<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Cocktail page</title>
  </head>
  <body>
    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= @cocktail.image %>');">
      <div class="container">
        <div class="container-left">
          <h1><%= @cocktail.name %></strong>!</h1>
        </div>

        <div class="box-container">

          <div>
            <h2>Ingredients</h2>
            <ul class="ingredient-list">
              <% @doses.each do |dose| %>
                <li class='list-bullet'>
                  <span><i class="fas fa-circle"></i><%= Ingredient.find(dose.ingredient.id).name %> - <%= dose.description %></span>
                  <span><%= link_to cocktail_dose_path(@cocktail.id, dose.id), method:"delete" do %><i class="fas fa-trash"></i><% end %></span>
                </li>
              <% end %>
            </ul>
          </div>

          <div class="delete">
            <a href="" >Cancel delete</a>
          </div>

        </div>


      </div>
    </div>
  </body>
</html>
